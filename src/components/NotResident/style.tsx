import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    width: 332,
  },

  description: {
    position: 'absolute',
    fontFamily: 'Starjedi',
    fontSize: 16,

    top: 30,
  },
});
